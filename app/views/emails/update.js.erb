console.log(document.querySelector('.btn-success').textContent.trim())


if (<%= @email.read %> == false) {
    document.querySelector('.btn-success').textContent = "Marquer comme lu";
    document.querySelector('ul .email-<%= @email.id %>').style.backgroundColor = "";
}
else {
    document.querySelector('.btn-success').textContent = "Marquer comme non-lu";
    document.querySelector('ul .email-<%= @email.id %>').style.backgroundColor = "grey";
}
